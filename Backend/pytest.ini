[pytest]
minversion = 8.0
addopts = -ra -q
testpaths = 
    tests
markers =
    devices: tests for /devices and /devices/{name} API
    xfail_devices_missing: temporary xfail until /devices endpoints are implemented
